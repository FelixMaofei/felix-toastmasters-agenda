interface:
  display_name: "明源云头马会单"
  short_description: "快速确认文字流程并生成A4会单图"
  brand_color: "#004165"
  default_prompt: "Use $mingyuan-toastmasters-agenda to parse this role-signup relay, quickly confirm the complete Markdown flow with me, then generate one accurate A4 agenda image and stop when I approve it."
policy:
  allow_implicit_invocation: true
