---
name: felix-toastmasters-agenda
description: 将 Toastmasters 角色接龙、会议说明或旧会单参考，转成内容准确、时间闭合、符合官方品牌规范的中文或英文会单。适用于各俱乐部的例会议程、角色接龙转会单、Toastmasters agenda、时间重算、A4 打印版和线上分享版；不复刻各俱乐部旧样式。
---

# Felix Toastmasters Agenda

## 核心价值

> **继承稳定内容，识别本期变化，自动完成加减计算，让整场会议重新闭合。**

输入是本期角色接龙、会议说明或可选旧会单；输出是内容准确、时间闭合、可现场执行的一页 A4 会单。旧会单只用于首次识别稳定信息，不复刻旧样式，也不把一家俱乐部的流程当成通用标准。

## 面向用户只有三个阶段

1. **信息确认**：只确认进入设计前确实缺少的关键信息，一次问清；可选增强不阻断初版。
2. **视觉初版**：必要信息齐全后不再停顿，也不要求用户审批整篇文字稿；内部完成计算并直接给真实 A4 图片。
3. **修改与交付**：用户看图后，可以一句话修改，也可以回复“直接导出”；确认交付时不重新设计和计算。

三层是系统内部的职责边界，不是三道审批门。用户始终知道当前进度、为什么需要回答，以及下一步会看到什么。

三层架构只供维护 Skill 时阅读；普通会单任务不加载架构文档。

## 默认面向 AI 小白

- 用户只需说“帮我把这份角色接龙做成会单”，不要求其准备旧会单、JSON、CSS、Python 或模板。
- 先读当前消息、图片、附件和文件名，已有事实不再问。
- 再按规范化俱乐部名称检查 `~/.toastmasters-agenda/profiles/`；地点只能辅助提示，不能单独认定俱乐部。
- 没有旧会单或 profile 也能初始化。先说已经识别出什么，再把真正影响准确性的缺项一次问完。
- 不使用逐题按钮、一次一个问题或字段式问卷。给用户一条可以直接照抄的简短回答格式。
- 必要问题与可选增强分开：必要项答完就进入视觉初版；可选项没答时使用当前材料或已说明的默认值，不继续追问。
- “你直接做”“生成吧”“我不懂配置”表示按当前已确认信息继续，不再追加非必要问题。
- 当前消息或 profile 已经明确存在必要缺项时，立即合并提问并结束本轮；不先建任务、读参考文档、检查环境、计算整场时间或创建文件。
- 日期中的星期只有用户提供或程序验证后才显示，不靠模型心算补充。
- 本期明确事实与 profile 不同时，本期事实直接优先；除非用户要求“以后默认这样”，不展开长期 profile 差异、不猜测代班关系，也不展示更新命令。
- 用户说“这期改成……”“字大一点”，已经是本期可逆调整的授权；不再追问是否允许改程序。
- 只有用户明确说“以后默认都这样”，才更新俱乐部 profile。
- 每次修改都保留上一份可用 PDF/PNG；失败版本不得覆盖成品。

### 信息确认怎么问

只有不同答案会改变会议承诺、又无法从材料或 profile 判断时，才列为必要问题，例如：

- 俱乐部身份、会议日期、开始时间或实际地点无法判断；
- 已出现的角色缺真实负责人；
- 特殊环节缺负责人、时长或相对位置；
- 时间计算必须超时，需要确认准确分钟数；
- 两种理解会产生不同流程，且当前材料无法取舍。

主题、今日一词、Pathways、俱乐部介绍、入会信息、二维码和更多固定组件通常是可选增强。固定信息组合未知时，说明本次先采用什么可用组合，不要求用户先做完整配置。

推荐提问格式：

> 我已经识别到：俱乐部、期数、会议主题和大部分角色。
> 在正式进入设计前，还需要确认 3 项必要信息：
> 1. ……
> 2. ……
> 3. ……
> 你可以直接回复：`1. ……；2. ……；3. ……`
> 另外，是否还有其他希望呈现在会单上的？例如 Pathways、俱乐部介绍、入会信息或二维码；不补也没关系，我会先按当前信息生成。你回复后，下一步直接给 A4 图片初版。

只有回答后仍不清楚内部字段怎样表达时，才读 [输入结构](references/input-schema.md)。

## V3 工作流

### 1. 信息确认与内部计算

从当前材料生成只包含本期事实的 `meeting.json`。保留接龙顺序，不手写完整时间轴、开始时间或转场。当前已明显缺必要事实时先提问，不创建 JSON；回答齐全后才继续。

### 2. 快速生成 A4 视觉初版

用户不填写视觉配置。普通任务不读取架构、视觉合同、源码、computed 结构或命令帮助；开始时只用一句进度说明：“必要信息已齐，正在计算和排版；下一条直接给 A4 图片初版。”随后只运行一个快路径命令：

```bash
python3 "$SKILL_DIR/scripts/run_agenda.py" first meeting.json \
  --club-profile "俱乐部完整名称" \
  --output-dir output
```

`first` 一次完成事实计算、内容与时间校验、默认视觉意图和 HTML/PDF/PNG 初版。退出 `2` 时只根据返回的真实缺项、冲突、剩余或超时分钟合并提问；退出 `0` 后直接展示图片，不再读取中间文件做第二轮理解。

必须查看真实 `agenda.preview.png`，至少检查整页和主流程原始尺寸。自动检查只能发现第二页、裁切、溢出和字号下限，不能证明好看。把图片初版直接交给用户，不展示技术过程或让用户继续等待文字稿。

### 3. 一次询问修改或交付

图片初版后只给一个合并入口：

> 这版已经可以直接交付。你可以回复“直接导出”，也可以一句话说修改，例如“字大一点”“加入俱乐部介绍”“总点评改 10 分钟”。
> 可改：会议内容、时长、负责人、顺序、显示列、重点、字号和信息组件。
> 固定底线：一页 A4、官方 Logo、时间重新闭合、不裁切隐藏内容。

用户说“直接导出”“生成吧”“OK”“就这样”都视为交付当前预览，不再追问。

```bash
python3 "$SKILL_DIR/scripts/run_agenda.py" final output/agenda.preview.html \
  --output-dir output
```

`final` 只校验并交付已经生成的预览文件，不能读取 meeting JSON、重新计算内容或重新选择视觉。收到直接导出后只运行这一条命令，不读文件、不建任务、不讨论 profile；成功后交付 `agenda.pdf` 和 `agenda.png`。内容反馈回到第一阶段重算；纯视觉反馈只重新生成第二阶段。

旧 `prepare / finalize` 只为 V2 历史回放保留；新任务不得调用。

## 视觉反馈怎么处理

- “整体字大一点”改 `design.text_scale`；不通过空卡、隐藏内容或整体缩放挤出空间。
- “重点太夸张 / 更突出”改 `content_emphasis`；重点仍与普通行共用全局列线，不按时长扩大面积。
- “显示 Pathways”只有计算真源已有对应数据时才加入 `display_columns`。
- “把俱乐部介绍放后面”改 `component_flow` 的语义分组或顺序，不写左右坐标和 CSS。
- “负责人没对齐、有死空白、列宽难看”属于 renderer 问题，不给 view 增加新字段。
- 纯视觉修改后，姓名、顺序、时长、地点和正文必须与已确认 `agenda.computed.json` 一致。

内容变化编辑 `meeting.json` 后重跑 `first`；纯视觉变化只编辑 `agenda.view.json` 并运行 `preview agenda.computed.json --view agenda.view.json`。不要重新研究整套流程。

视觉目标只有：整齐、清晰、简约高级、字能看清。重点、对齐、留白、对比优先；留白放在内容组之间，不封在有边框组件里。日常模型不得每期自由重写 HTML/CSS。

## 绝不能破的不变项

- 姓名已填入本期接龙就代表本期承诺，不再审问是否同意。
- 本期明确事实优先于 profile 和通用默认；视觉层不得改写事实。
- 角色行不存在就不生成其触发环节；幕后角色不自动创造台前环节。
- 特殊环节必须有真实姓名负责人；“Toastmaster”“主持人”“待定”不是姓名。
- 备稿演讲与点评按编号绑定。默认需要点评，只有本期明确取消才关闭。
- 时间由程序按 0.5 分钟步进重算；默认会议 120 分钟，超时必须确认准确分钟数。
- Markdown、HTML、PDF 和 PNG 来自同一份计算真源，不用图片模型重排密集文字。
- PDF 必须且只能是一页 A4 竖版。不跨页、不裁切、不隐藏、不缩成无法现场阅读的小字。
- 只使用 Toastmasters International 官方 Logo / Wordmark / Logo Lockup，不重设、不改色、不拉伸、不加特效。
- 最终成品不包含提示词、内部判断、缺失项、历史文字或制作过程备注。

## 完成回复

面向普通用户只说明：会单已完成、时间是否闭合、本轮要求是否落实，并交付 PDF/PNG。不要罗列 renderer、JSON、HTML、profile 路径或检查过程。只有长期设置确实写入成功时，才说“以后会默认沿用”。
