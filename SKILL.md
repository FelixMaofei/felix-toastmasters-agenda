---
name: felix-toastmasters-agenda
description: 将 Toastmasters 角色接龙、会议说明或旧会单参考，转成时间重新闭合的一页 A4 中文、英文或双语会单。用于例会议程、角色接龙转会单、时间重算、自然语言修改以及 PDF/PNG 交付。
---

# Felix Toastmasters Agenda

核心价值：继承俱乐部稳定内容，识别本期变化，自动完成加减计算，让整场会议重新闭合。

用户只需提供角色接龙、本期说明和自然语言修改。Agent 负责确认信息和调用程序；时间计算、排版与文件检查交给程序。

## 1. 内容确认

### 1.1 识别并逐步补充

先说明已识别的俱乐部、日期时间、角色、演讲、特殊环节和当前进度。只问会影响负责人、流程、文字内容或时间闭合，且当前无法判断的问题。用户可以通过多轮对话逐步补充；每轮都承接已经确认的内容，不重复追问。相关且容易回答的问题可以一起问，但不要为了“一次完成”把沟通变成表单。

- 特殊环节必须有标题、负责人、时长和相对位置。
- 保留即兴演讲时，每期必须确认即兴时长；有即兴点评时也必须确认点评时长，可以先建议为即兴时长的一半。
- 未给合影休息或真情分享时长时，明确问：`我为你默认安排了合影＋休息10分钟，真情分享10分钟，你觉得 OK 吗？` 用户确认后才写入本期安排；不得用剩余时间静默改变。
- 没有“真情分享”角色时只确认一次：`有：负责人姓名；没有：取消。`
- 事务官开场、会议规则和规则介绍视为同一环节。点评必须位于对应演讲之后；总点评与真情分享都在收尾区，两者先后按用户需要。

角色接龙里的活动介绍、宣传亮点、邀请文案和群公告话术默认只用于理解主题，不进入会单。主题、今日一词等明确字段正常保留；只有用户明确要求时才展示宣传全文。

俱乐部介绍、官员名单、真实入会方式、二维码和 Pathways 属于可选内容。没有真实资料就不展示，也不生成占位话术。用户说“没有其他内容”或“纯会单”时，不继承任何可选信息。

俱乐部简介、常用地点、会单语言、官员名单、真实入会方式和入会二维码可以保存为 profile。用户首次提供或更新这些内容时，合并询问：

> 这些属于俱乐部长期资料。是否为你保存？保存后，下次做会单不用再重复提供，只需告诉我本期角色接龙和变化。

用户选择保存时才更新已有 profile；主题、演讲、当期角色、临时地点和特殊环节永远不写入 profile。本期明确事实始终优先于 profile。

Skill 可以内置经过授权公开的俱乐部 profile。命中内置 profile 时直接沿用并告诉用户下次只需提供本期角色与变化；用户后续确认的本地 profile 优先于内置版本。

每轮问题都尽量具体，并给简单的回答示例。信息接近齐全时询问：

> 是否还有其他希望呈现在会单上的？

### 1.2 生成完整文字确认稿

必要信息齐全后，把本期事实写入内部 `simple_version: 1` JSON。只需要 `club / meeting / roles / speeches / impromptu / backstage / special / agenda_overrides`；用户不接触 JSON。

运行：

```bash
python3 "$SKILL_DIR/scripts/run_agenda.py" confirm meeting.json \
  --club-profile "俱乐部完整名称" --output-dir output
```

用户明确要求保存长期资料时增加 `--update-club-profile`。程序自行查找 profile；Agent 不预读 profile、源码、目录或命令帮助。

程序检查后，按实际结果继续和用户沟通：

- 信息仍不足（`needs_input`）：说明还缺什么，继续对话，不生成图片。
- 计算发现超时：告诉用户准确超时多久、预计几点结束，请用户选择延长或调整内容；未同意前不继续。
- 文字稿已经准备好（`text_confirmation_ready`）：完整展示 `agenda.md`，请用户确认，不能只给摘要。
- 俱乐部长期资料刚刚保存成功（`profile.status` 为 `created` 或 `updated`）：告诉用户真正记住了哪些资料，以及下次可以少提供什么。`reused` 只表示本期沿用了旧资料，不能说成刚刚保存。
- 命中 Skill 内置公开资料（`profile.status` 为 `bundled`）：说明已自动使用哪些俱乐部资料；不能声称写入了用户本机。

文字确认稿必须包含最终会单会出现的基本信息、完整流程、负责人、时长、演讲题目、Pathways、俱乐部介绍、官员名单、入会信息和时间闭合结果。用户确认前不生图。

## 2. 文字确认后生图

用户明确回复“确认出图”“内容确认”或同义表达后，只使用文字确认稿对应的 `agenda.computed.json` 和 `facts_sha256`：

```bash
python3 "$SKILL_DIR/scripts/run_agenda.py" image output/agenda.computed.json \
  --confirmed-sha256 "程序返回的 facts_sha256" --output-dir output
```

`image` 不再解析角色接龙、不读取 profile、不重新计算内容。文字发生任何变化，旧编号立即失效，必须回到内容确认。返回 `preview_ready` 后直接展示 `agenda.preview.png`，并保留对应 PDF。

## 3. 修改与交付

- 姓名、顺序、时长或正文变化：更新本期输入，回到 `confirm`，展示新的完整文字稿。
- 纯视觉变化：使用最小 `view.patch.json` 重新运行 `image`，不要求用户重复确认未变化的文字。
- 用户说“直接导出”“可以”“OK”或“就这样”：只运行 `final`，原样交付用户已经看到的 PDF/PNG，不重新排版。

```bash
python3 "$SKILL_DIR/scripts/run_agenda.py" final \
  output/agenda.preview.html --output-dir output
```

## 不变要求

- 程序只根据已确认时长计算开始时间、结束时间、转场和差额，不反推即兴、合影休息或真情分享时长。
- 成品为一页 A4，使用官方 Logo，清晰可读，不裁切、不隐藏。
- 支持中文、英文和中英双语。
- 最终回复只说明会单已完成、时间是否闭合、修改是否落实，并交付 PDF/PNG。
